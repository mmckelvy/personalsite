(function () {

	var centerNavEls = function () {

		// Get the width of the nav container.
		// Get the widths of all the nav els.
		// Get the number of nav els.
		// Find the distribution space by subtracting the sum of the el widths from the container width, and diving by # of els - 1. 

	};


})();